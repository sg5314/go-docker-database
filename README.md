# go-docker-database

```
docker-compose build
docker compose up -d 
```

```
docker exec -it db /bin/bash
mysql -u{root} -p{???}
```

```
docker-compose exec server sh
```

```
docker-compose down -v
```